spring:
  batch:
    initialize-schema: always
    job:
      enabled: false
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/users_db
    username: root
    password:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      dialect: org.hibernate.dialect.MySQL5Dialect
input1: file:/I:/2022/IntelliJ Workspace/etl-spring-batch-processing/src/main/resources/users.csv
input2: file:/I:/2022/IntelliJ Workspace/etl-spring-batch-processing/src/main/resources/electronic-card-transactions.csv